// place files you want to import through the `$lib` alias in this folder.
import socketService from "./services/socketService"
import { gameStore } from '$lib/stores/gameStore';
export type { JoinRoom, SubmitForm } from './types';